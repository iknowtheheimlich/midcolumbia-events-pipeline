def dedupe(events):
    seen = set()
    out = []

    for e in events:
        key = e["link"] or (
            e["title"],
            e["venue"],
            e["start_date"]
        )

        if key in seen:
            continue

        seen.add(key)
        out.append(e)

    return out

clean_events = dedupe(normalized)
print(f"After dedupe: {len(clean_events)} events")
